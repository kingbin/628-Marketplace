---
description: Deploy the 628-Marketplace CMC source into the live install and restart the server
allowed-tools: Bash(~/bin/cmc-deploy), Bash(~/bin/cmc-deploy --dry-run)
---

Deploy the latest CMC plugin source from the 628-Marketplace repo into the live
install and force-restart the server, then report the result.

!`~/bin/cmc-deploy`
